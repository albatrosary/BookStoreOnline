import { TelDirective } from './tel.directive';

describe('TelDirective', () => {
  it('should create an instance', () => {
    const directive = new TelDirective();
    expect(directive).toBeTruthy();
  });
});
